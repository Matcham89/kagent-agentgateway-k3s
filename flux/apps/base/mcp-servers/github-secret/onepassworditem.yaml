---
# yaml-language-server: $schema=https://raw.githubusercontent.com/1Password/onepassword-operator/main/config/crd/bases/onepassword.com_onepassworditems.yaml
apiVersion: onepassword.com/v1
kind: OnePasswordItem
metadata:
  name: github-mcp-server
spec:
  # Materialised as Secret "github-mcp-server" with key
  # HOLMESGPT_GITHUB_PAT. Reuses the SAME 1Password item as the original
  # holmesgpt/github-secret (now removed — github-mcp-server moved here so
  # it isn't tied to the holmesgpt Helm release/namespace).
  itemPath: "vaults/wlgvy5jij6zg2sb25nju4nyjnm/items/HOLMESGPT_GITHUB_PAT"
