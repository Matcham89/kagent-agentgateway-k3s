---
apiVersion: kagent.dev/v1alpha2
kind: AgentHarness
metadata:
  name: scout-test
  namespace: kagent
spec:
  backend: hermes
  modelConfigRef: default-model-config
  description: Agent harness with access to shell and qdrant tools
  substrate:
    workerPoolRef:
      name: kagent-default
