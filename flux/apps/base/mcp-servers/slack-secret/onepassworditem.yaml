---
# yaml-language-server: $schema=https://raw.githubusercontent.com/1Password/onepassword-operator/main/config/crd/bases/onepassword.com_onepassworditems.yaml
apiVersion: onepassword.com/v1
kind: OnePasswordItem
metadata:
  name: slack-mcp-server
spec:
  # Materialised as Secret "slack-mcp-server" with key
  # HOLMESGPT_SLACK_BOT_TOKEN. Reuses the SAME 1Password item as
  # holmesgpt/slack-secret/onepassworditem.yaml — Secrets are namespace
  # scoped, so this is a second OnePasswordItem pointing at the identical
  # vault item to land a copy here for the relocated slack-mcp-server pod.
  itemPath: "vaults/wlgvy5jij6zg2sb25nju4nyjnm/items/HOLMESGPT_SLACK_BOT_TOKEN"
