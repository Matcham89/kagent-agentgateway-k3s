---
# yaml-language-server: $schema=https://json.schemastore.org/kustomization
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: flux-system
# No infra/namespace component here — flux-system is created by the manual
# bootstrap `helm install`, before Flux (and its GitOps-managed PSS labels) exist.
resources:
  - ./flux-operator/ks.yaml
